"""SQLAlchemy ORM models: User, DownloadRequest.

STUB — implemented by Agent C (M3) per the schema in ARCHITECTURE.md §6.
Class names `User` and `DownloadRequest` are FROZEN (referenced by repo.py).
"""
from __future__ import annotations
